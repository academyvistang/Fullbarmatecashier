@model BarAndRestaurantMate.Models.HotelMenuModel



@if (Model.AllOpenOrdersGroupBy.Any())
{
    <div class="row">

        <div class="twelve columns" >
            @foreach (var r in Model.AllOpenOrdersGroupBy)
            {
                <p>@r.ActualGuestOrder.BarTable.TableName</p>
                <div id="tableOrderDiv">
                @Html.Partial("_TableOrderOpen", r)
                </div>
                <br />
                <hr />
            }
        </div>

    </div>
}
else
{
    <div class="row">
        <div class="twelve columns" style="padding-left:30px;">
            <p>There are no table orders.</p>
        </div>
    </div>
}
