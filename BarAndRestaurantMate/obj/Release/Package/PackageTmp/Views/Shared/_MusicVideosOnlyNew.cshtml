@model BarAndRestaurantMate.Models.GuestRoomAccountViewModel
<link href="~/css/TilesSmall.css" rel="stylesheet" />

<div class="wrap">

    <!-- Define all of the tiles: -->
    

    @foreach (var m in Model.CinemaList)
    {
        <a href="@Url.Action("ShowFilm", "Guest", new { id = m.Id })">
        <div class="box">
            <div style="width:95%;padding-left:5%;">
                <input type="text" value="@m.MovieName" style="width:100%; font-size:10px; text-align:center;" title="@m.MovieName" />
            </div>
        <div class="boxInner">
            @{var val = m.Id.ToString() + ".jpg";}
            <img src="~/Videos/Films/@val" />
            <div class="titleBox">@m.MovieName</div>
        </div>
    </div></a>

        

    }
</div>

