@model BarAndRestaurantMate.Models.PersonEmailViewModel

<section id="content" class=" twelve columns">

    @if (Model.ItemSaved.HasValue && Model.ItemSaved.Value)
    {
        @Html.Partial("_EntityUpdated", "Person details updated.")
    }

    <h1 class="page-title ci-title"><span>Guest</span></h1>

    <article id="post-16" class="post-16 page type-page status-publish hentry entry group">

        <div class="booking-form-wrap bs bg">

            @using (Html.BeginForm("NewPerson", "Account", FormMethod.Post, new { enctype = "multipart/form-data", @class = "booking-form b-form row" }))
            {
                <span style="color:red;">@Html.ValidationSummary()</span>
                @Html.Partial("_PersonFullQuickEmail")
            }

        </div>

    </article>

</section>


